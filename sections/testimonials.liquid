{%- comment -%}
  TESTIMONIALS SECTION
  Customer reviews/testimonials - Luxe style
{%- endcomment -%}

<section class="section testimonials">
  <div class="container">
    <header class="testimonials__header reveal">
      {%- if section.settings.subtitle != blank -%}
        <p class="savoir-faire__subtitle">{{ section.settings.subtitle }}</p>
      {%- endif -%}
      {%- if section.settings.title != blank -%}
        <h2>{{ section.settings.title }}</h2>
      {%- endif -%}
    </header>

    <div class="grid grid--3 reveal-stagger">
      {%- for block in section.blocks -%}
        <div class="testimonial" {{ block.shopify_attributes }}>
          {%- if block.settings.stars > 0 -%}
            <div class="testimonial__stars">
              {%- for i in (1..block.settings.stars) -%}★{%- endfor -%}
            </div>
          {%- endif -%}
          <blockquote class="testimonial__quote">
            "{{ block.settings.quote }}"
          </blockquote>
          <p class="testimonial__author">{{ block.settings.author }}</p>
          {%- if block.settings.location != blank -%}
            <p class="testimonial__location">{{ block.settings.location }}</p>
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Témoignages",
  "settings": [
    {
      "type": "text",
      "id": "subtitle",
      "label": "Sous-titre",
      "default": "Avis clients"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Titre",
      "default": "Ce que disent nos clientes"
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Témoignage",
      "settings": [
        {
          "type": "range",
          "id": "stars",
          "min": 0,
          "max": 5,
          "step": 1,
          "label": "Étoiles",
          "default": 5
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Citation",
          "default": "Un chèche d'une douceur incroyable ! La qualité est au rendez-vous."
        },
        {
          "type": "text",
          "id": "author",
          "label": "Auteur",
          "default": "Marie D."
        },
        {
          "type": "text",
          "id": "location",
          "label": "Lieu",
          "default": "Paris"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Témoignages",
      "blocks": [
        {
          "type": "testimonial",
          "settings": {
            "stars": 5,
            "quote": "Un chèche d'une douceur incroyable ! La qualité est au rendez-vous et je reçois des compliments à chaque fois que je le porte.",
            "author": "Marie D.",
            "location": "Paris"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "stars": 5,
            "quote": "J'adore le fait que chaque pièce soit unique. Le mien est numéroté et ça lui donne une valeur particulière.",
            "author": "Sophie L.",
            "location": "Lyon"
          }
        },
        {
          "type": "testimonial",
          "settings": {
            "stars": 5,
            "quote": "Excellent rapport qualité-prix pour du fait-main français. L'emballage était soigné, une vraie attention aux détails.",
            "author": "Claire M.",
            "location": "Bordeaux"
          }
        }
      ]
    }
  ]
}
{% endschema %}
